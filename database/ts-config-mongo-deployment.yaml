apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-config-mongo
  name: ts-config-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-config-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-config-mongo
      name: ts-config-mongo
    spec:
      containers:
        - name: ts-config-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
