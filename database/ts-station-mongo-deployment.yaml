apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-station-mongo
  name: ts-station-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-station-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-station-mongo
      name: ts-station-mongo
    spec:
      containers:
        - name: ts-station-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
