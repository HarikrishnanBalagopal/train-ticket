apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-order-mongo
  name: ts-order-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-order-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-order-mongo
      name: ts-order-mongo
    spec:
      containers:
        - name: ts-order-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
