apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-order-other-mongo
  name: ts-order-other-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-order-other-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-order-other-mongo
      name: ts-order-other-mongo
    spec:
      containers:
        - name: ts-order-other-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
