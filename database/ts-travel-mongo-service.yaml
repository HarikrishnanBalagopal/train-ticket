apiVersion: v1
kind: Service
metadata:
  labels:
    move2kube.konveyor.io/service: ts-travel-mongo
  name: ts-travel-mongo
spec:
  type: ClusterIP
  selector:
    move2kube.konveyor.io/service: ts-travel-mongo
  ports:
    - name: port-27017
      protocol: TCP
      port: 27017
      targetPort: 27017
