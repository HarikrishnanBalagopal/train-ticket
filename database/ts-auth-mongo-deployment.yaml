apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-auth-mongo
  name: ts-auth-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-auth-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-auth-mongo
      name: ts-auth-mongo
    spec:
      containers:
        - name: ts-auth-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
