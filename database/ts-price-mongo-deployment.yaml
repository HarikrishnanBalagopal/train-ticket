apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-price-mongo
  name: ts-price-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-price-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-price-mongo
      name: ts-price-mongo
    spec:
      containers:
        - name: ts-price-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
