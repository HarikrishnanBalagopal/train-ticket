apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-security-mongo
  name: ts-security-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-security-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-security-mongo
      name: ts-security-mongo
    spec:
      containers:
        - name: ts-security-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
