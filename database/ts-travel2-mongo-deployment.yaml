apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-travel2-mongo
  name: ts-travel2-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-travel2-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-travel2-mongo
      name: ts-travel2-mongo
    spec:
      containers:
        - name: ts-travel2-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
