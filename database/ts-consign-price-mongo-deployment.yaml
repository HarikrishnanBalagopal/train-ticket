apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-consign-price-mongo
  name: ts-consign-price-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-consign-price-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-consign-price-mongo
      name: ts-consign-price-mongo
    spec:
      containers:
        - name: ts-consign-price-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
