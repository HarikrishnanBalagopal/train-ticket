apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-travel-mongo
  name: ts-travel-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-travel-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-travel-mongo
      name: ts-travel-mongo
    spec:
      containers:
        - name: ts-travel-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
