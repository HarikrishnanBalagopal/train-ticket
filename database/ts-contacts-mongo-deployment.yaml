apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-contacts-mongo
  name: ts-contacts-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-contacts-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-contacts-mongo
      name: ts-contacts-mongo
    spec:
      containers:
        - name: ts-contacts-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
