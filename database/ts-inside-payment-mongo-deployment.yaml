apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    move2kube.konveyor.io/service: ts-inside-payment-mongo
  name: ts-inside-payment-mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      move2kube.konveyor.io/service: ts-inside-payment-mongo
  template:
    metadata:
      labels:
        move2kube.konveyor.io/service: ts-inside-payment-mongo
      name: ts-inside-payment-mongo
    spec:
      containers:
        - name: ts-inside-payment-mongo
          image: mongo
          imagePullPolicy: Always
          ports:
            - containerPort: 27017
              protocol: TCP
      restartPolicy: Always
